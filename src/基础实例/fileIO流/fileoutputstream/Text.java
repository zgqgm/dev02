package 基础实例.fileIO流.fileoutputstream;

import java.io.File;
import java.io.FileOutputStream;
import java.io.IOException;

public class Text {
    public static void main(String[] args) throws IOException {
        /*File file = new File("./src/bbb.txt");
        FileOutputStream fos = new FileOutputStream(file, true);//字节输出流追加写（不写true或写false为删除写）
        fos.write(98);//ASCLL值
        fos.write("\r\n天天晚上emo".getBytes());//追加字符串
        fos.write("\r\nabcded".getBytes(), 3, 4);//截取后追加写
        fos.close();*/
    }
}
