package 基础实例.fileIO流.自定义;

import java.io.*;

public class Test {
    public static void main(String[] args) throws IOException, ClassNotFoundException {
        /*person per=new person();
        per.setAge(18);
        per.setName("小明");
        per.setGender('男');
        per.setID(1);
        //创建对象输出流（字节）
        ObjectOutputStream oos=new ObjectOutputStream(new FileOutputStream(new File("C:/Users/zgq/Desktop/测试文件夹/1.txt")));
        //写入对象
        oos.writeObject(per);
        oos.close();*/
        //创建对象输入流（字节）
        /*ObjectInputStream ois=new ObjectInputStream(new FileInputStream(new File("C:/Users/zgq/Desktop/测试文件夹/1.txt")));
        person pres=(person) ois.readObject();
        System.out.println(pres);
        ois.close();*/
    }
}
